from setuptools import setup, find_packages

setup(
    name='pyphonopy',
    version='0.1',
    packages=find_packages(),
    description='a pure python implementation of phonopy',
    requires=['numpy'],
)
